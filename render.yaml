services:
  - type: web
    name: ai-research-paper-intelligence-platform
    env: python
    rootDir: backend
    buildCommand: pip install -r mydeps.txt
    startCommand: gunicorn app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9